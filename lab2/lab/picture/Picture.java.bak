/**
 * This class represents a simple picture. You can draw the picture using
 * the draw method. But wait, there's more: being an electronic picture, it
 * can be changed. You can set it to black-and-white display and back to
 * colors (only after it's been drawn, of course).
 *
 * This class was written as an early example for teaching Java with BlueJ.
 * 
 * @author  Michael Kolling and David J. Barnes
 * @version 2006.03.30
 */
public class Picture
{
    private Square wall;
    private Square window;
    private Triangle roof;
    private Circle sun;

    /**
     * Constructor for objects of class Picture
     */
    public Picture()
    {
        // nothing to do... instance variables are automatically set to null
    }

		/**
		 * Realiza las instrucciones descritas en el ejercicio 5
		 */
		public void ejercicio2a()
		{
	  	//Define las variables, objetos e instrucciones necesarias
	  }
	  
		/**
		 * Realiza las instrucciones descritas en el ejercicio 6
		 */
		public void ejercicio2b()
		{
	  	//Define las variables, objetos e instrucciones necesarias
	  }	  
	  


    /**
     * Dibuja una casa y el sol.
     */
    public void dibujar()
    {
    	  //crea una pared (wall), asignale el tamaño adecuado y muevelo a su sitio correspondiente

        
    	  //crea una ventana (window), asignale el tamaño y el color adecuado, y muevelo a su sitio correspondiente

    	  //crea un tejado (roof), asignale el tamaño adecuado y muevelo a su sitio correspondiente

    	  //crea un sol (sun), asignale el tamaño y el color adecuado, y muevelo a su sitio correspondiente
    }

    /**
     * Change this picture to black/white display
     */
    public void setBlackAndWhite()
    {
        if(wall != null)   // only if it's painted already...
        {
            wall.changeColor("black");
            window.changeColor("white");
            roof.changeColor("black");
            sun.changeColor("black");
        }
    }

    /**
     * Change this picture to use color display
     */
    public void setColor()
    {
        if(wall != null)   // only if it's painted already...
        {
            wall.changeColor("red");
            window.changeColor("black");
            roof.changeColor("green");
            sun.changeColor("yellow");
        }
    }
}
